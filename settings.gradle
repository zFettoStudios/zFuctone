pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()

        maven { url = papermc_repository }
    }
}

rootProject.name = project_name

include module_minecraft_bukkit